---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Hey there 👋</div>
    <div class="text-5xl font-bold">I'm Athang Gupte</div>
    <div class="text-3xl py-3 font-bold">Game Engine Developer & Software Engineer</div>
    <div class="py-2">
      <text class="text-lg">
        Passionate computer scientist with a track record of innovation and excellence in both gaming and real-world applications. From revolutionizing animation technology at <b>PlayStation's Santa Monica Studio</b> to creating a 3D game engine from scratch, I thrive on pushing the boundaries of technology with code.
      </text>
    </div>
    <div class="mt-8">
      <a class="btn" href="mailto:athang213@gmail.com" target="_blank"> Let's connect!</a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">My latest projects {"</>"}</div>
  </div>

  <HorizontalCard
    title="Apex Game Engine"
    img="/post_img.webp"
    desc="In-house custom C++, OpenGL/Vulkan game engine."
    url="#"
    badges={["Game-dev", "Software Engineering"]}
    tags={["C++20", "OpenGL", "Vulkan", "GLSL", "Lua scripting"]}
  />
  <div class="divider my-0"></div>
  <HorizontalCard
  title="SENVA-2 (NASA SUITS)"
  img="/post_img.webp"
  desc="MXR-based HUD and digital voice assistant for aiding NASA's next batch of astronauts 🧑‍🚀 during Extra-Vehicular Activities on the Moon 🌙"
  url="#"
  badges={["NLP", "AI", "Mixed Reality"]}
  tags={["Unity", "C#"]}
  />
  <div class="divider my-0"></div>
  <a href="/projects" >View all projects...</a>


  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + post.slug}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
